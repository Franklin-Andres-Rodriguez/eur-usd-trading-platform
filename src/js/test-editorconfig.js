function testEditorConfig() {
    // Esta línea debe tener exactamente 4 espacios de indentación
    const eurUsdRate = 1.0847;

    if (eurUsdRate > 1.08) {
        // Esta línea debe tener 8 espacios de indentación
        console.log('EUR is strong against USD');
    }
}
